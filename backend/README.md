# Backend (Next.js)

This folder contains a minimal Next.js backend exposing a single API route:

- `GET /api/hello` — returns a JSON message and timestamp.

How to run (from this `backend` folder):

```bash
npm install
npm run dev
```

The API will be available on `http://localhost:3000/api/hello` by default.
